#include <stdio.h>

int main()
{

    char name[10];
    int age;
    int num;

    scanf("%s", &name);
    printf("name  : %s", name);

    scanf("%d", age);
    printf("age  : %d", age);

    scanf("%d", num);
    printf("name  : %d", num);

    return 0;
}
