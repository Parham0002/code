#include <stdio.h>

int main(){
unsigned int num;
printf("write your number for the sum:");
scanf("%d", &num);

if (num / 2))
{
    
}

printf("this is sum of your even number: %d", num);

    return 0;
}