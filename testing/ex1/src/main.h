#ifndef MAIN_H
#define MAIN_H

#include <stdint.h>
#include <stdbool.h>

/**
 * @brief This function is used to check if number devidev by 3 or 5 or both
 *
 * @param n The number to check
 * @return true if the number is divisible by 3 or 5 or both, false otherwise
 */
void fizzbuzz(int n);

#endif // "MAIN_H"