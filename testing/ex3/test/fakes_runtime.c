// Simulation for runtime substitution
// (You can actually make it more dynamic with function pointers for real runtime faking)

void use_runtime_fake()
{
    // Simulated by reassigning the fakes if needed
}
